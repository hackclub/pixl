node_modules

out
dist
*.tgz

# Link-preview cards built by `bun run previews:build` during the deploy
# workflow. The index.html files next to these stay tracked - the build only
# injects a marked meta block into them.
apps/game/web/og.png
apps/game/web/*/og.png
apps/game/web/*/*/og.png

coverage
*.lcov

logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

.env
.env.development.local
.env.test.local
.env.production.local
.env.local

.eslintcache
.cache
*.tsbuildinfo

.idea

.DS_Store

# Scratch mockups from the superpowers brainstorming visual companion
.superpowers/

samples/
# per-machine Claude Code config (permissions, MCP toggles)
.claude/settings.local.json
